This ATM generates "Updates_log" PR-s for all projects in the `names.txt` file


ToDo "Merge " with "update.php" 
E.g. split "common" functionality into different parts so they can be used elsewhere

